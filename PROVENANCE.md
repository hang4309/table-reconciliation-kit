# Provenance

The `dqrdesk` Python source and MIT copyright notice are reused from asset 0124, Data Quality Reconciliation Desk, produced in an AI-assisted code asset collection in 2026. The existing source was copied without functional edits for this initial publication.

The original code was generated in a pipeline using Codex models. This publication selects a narrow use, adds a fresh synthetic example, an independent example runner, a visual explanation and explicit validation limits. It does not inherit the pipeline's completion or acceptance labels.

The sample CSV/JSON inventory records and the case study were created for this publication work. They do not contain customer data. No private production handoff, account information, local-machine paths, historical state, archive manifest or original demand text is included.

Source comments and inherited features remain subject to review before further use. This publication is not a full security audit, financial certification, or verification of every format and review action in the engine.
